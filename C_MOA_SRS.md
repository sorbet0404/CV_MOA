# 📄 소프트웨어 요구사항 명세서 (SRS)

**프로젝트명:** CV_MOA (C MOA)
**버전:** 1.3 **작성일:** 2025년 12월 24일

---

## 1. 소개 (Introduction)

### 1.1 목적 (Purpose)
본 문서는 구직자와 기업 채용 담당자를 연결하는 양방향 매칭 플랫폼 **'C_MOA'**의 기능 및 비기능 요구사항을 정의한다.
본 시스템은 기존의 정적인 채용 방식을 탈피하여, **'스와이프(Swipe)'**라는 직관적인 UI와 **'양방향 탐색(Dual Discovery)'** 모델을 통해 신속하고 효율적인 인재 매칭 경험을 제공하는 것을 목적으로 한다.

### 1.2 범위 (Scope)
* **구직자 (Seeker):** 기업 공고 카드를 스와이프하여 지원 의사를 밝히고, 매칭 시 담당자와 채팅한다.
* **채용 담당자 (Recruiter):** 구직자의 프로필 카드를 스와이프하여 서류 합격 의사를 밝힌다.
* **플랫폼:** 모바일 웹(Mobile Web) 환경을 우선으로 하며, 반응형 웹을 지원한다.

---

## 2. 사용자 정의 (User Roles)

| 사용자 유형 | 설명 | 주요 활동 |
| :--- | :--- | :--- |
| **구직자 (Seeker)** | 일자리를 찾는 개인 사용자 | 프로필 등록, 기업 공고 스와이프, 채팅 |
| **채용 담당자 (Recruiter)** | 인재를 찾는 기업 사용자 | 채용 공고 등록, 인재 스와이프, 채팅 |
| **관리자 (Admin)** | 시스템 운영자 | 부적절한 유저/공고 관리, 신고 처리 |

---

## 3. 기능 요구사항 (Functional Requirements)

### 3.1 회원가입 및 인증 (Authentication & Session)
* **FR-Auth-01 (가입):** 시스템은 이메일 및 OAuth 2.0(Google, Kakao, GitHub) 기반의 사용자 인증 및 가입 처리를 지원해야 한다.
* **FR-Auth-02 (기업 인증):** '채용 담당자' 역할로 가입 요청 시, 서버는 입력된 **기업 코드(Corporate Code)**의 유효성을 검증하고, 유효한 경우에만 가입을 승인해야 한다.
* **FR-Auth-03 (세션 관리):**
    * 시스템은 보안을 위해 모든 세션의 유효 시간을 최대 30분으로 설정한다.
    * API 요청 등 사용자 활동이 감지될 때마다 세션 만료 시간을 30분으로 재설정(Extension)하는 로직을 수행한다.
    * 세션 만료 시 모든 인증 토큰을 무효화한다.

### 3.2 프로필 및 공고 관리 (Profile & Job Posting)
* **FR-Profile-01 (구직자 데이터 관리):** 시스템은 구직자의 기술 스택, 경력 연차, 자기소개, 포트폴리오 URL, 대표 이미지 데이터를 생성(Create) 및 수정(Update)할 수 있는 API를 제공한다.
* **FR-Job-01 (공고 데이터 관리):** 시스템은 채용 담당자가 생성한 기업 정보, 직무 설명(JD), 우대 사항, 연봉 정보를 포함한 공고 데이터를 저장하고 관리한다.

### 3.3 매칭 핵심 로직 (Matching Core Logic)
* **FR-Match-01 (매칭 의사 처리):** 시스템은 사용자의 매칭 의사(Like/Dislike) 요청을 받아 `SWIPE_LOGS`에 저장한다.
* **FR-Match-02 (재노출 제한 알고리즘):**
    * 'Dislike' 로그가 생성된 대상은 **72시간(3일)** 동안 추천 목록 쿼리에서 제외되어야 한다.
    * 72시간 경과 후 대상이 다시 조회될 경우, 응답 데이터에 **'과거 Dislike 기록 있음'** 플래그를 포함해야 한다.
* **FR-Match-03 (상호 매칭 트리거):**
    * 'Like' 요청 발생 시, 상대방도 나에게 'Like'를 보냈는지 확인하는 로직을 수행한다.
    * 쌍방 'Like'가 확인되면 즉시 `MATCHES` 테이블에 관계 데이터를 생성하고, 매칭 상태를 'Matched'로 변경한다.
* **FR-Match-04 (정보 공개 처리):** 매칭(`MATCHES`) 데이터가 생성되는 시점에, 시스템은 구직자의 비공개 연락처 정보를 채용 담당자가 조회할 수 있는 상태로 권한을 변경한다.

### 3.4 탐색 및 데이터 쿼리 모드 (Discovery Logic)

#### **A. 구직자용 쿼리 (Seeker Query Logic)**
* **FR-Seeker-01 (받은 제안 모드 - Inbound):**
    * 나(구직자)에게 'Like'를 보낸 채용 공고 ID 목록을 필터링하여 반환한다.
* **FR-Seeker-02 (전체 공고 탐색 모드 - Outbound):**
    * 전체 채용 공고 중 내가 아직 처리(Like/Dislike)하지 않은 공고 목록을 반환한다.
* **FR-Seeker-03 (기업 상세 보기 - Company Detail):**
    * 특정 공고 ID에 대한 상세 기업 정보(평점, 주요 산업, 장단점 포함)를 반환한다.

#### **B. 채용 담당자용 쿼리 (Recruiter Query Logic)**
* **FR-Recruiter-01 (받은 지원 모드 - Inbound):**
    * 우리 기업 공고에 'Like'를 보낸 구직자 ID 목록을 필터링하여 반환한다.
* **FR-Recruiter-02 (인재 풀 탐색 모드 - Outbound):**
    * 전체 구직자 중 내가 아직 처리하지 않은 구직자 목록을 반환한다.
* **FR-Recruiter-03 (인재 상세 보기 - Seeker Detail):**
    * 특정 구직자 ID에 대한 상세 스펙(GitHub 활동 지수, 기술 스택, 상세 경력)을 반환한다.

### 3.5 채팅 시스템 (Chat System Logic)
* **FR-Chat-01 (방 생성):** `MATCHES` 테이블에 데이터가 생성되는 시점에, 해당 매칭 ID와 연결된 고유한 채팅방(`CHAT_ROOMS`) 데이터를 생성하거나 활성화한다.
* **FR-Chat-02 (메시지 처리):**
    * 시스템은 전송된 텍스트 메시지를 `CHAT_MESSAGES` 테이블에 저장한다.
    * 메시지 조회 시 `sent_at` 기준 오름차순으로 정렬된 데이터를 반환한다.
* **FR-Chat-03 (목록 조회):**
    * 사용자 ID를 기준으로 참여 중인 채팅방 목록을 조회한다.
    * 상대방 정보(구직자는 기업 정보, 담당자는 구직자 정보)를 조인(Join)하여 함께 반환한다.
* **FR-Chat-04 (파일 처리):** 시스템은 이미지 및 PDF 파일 업로드 요청을 처리하고, 저장된 파일의 접근 URL을 메시지 타입으로 반환할 수 있어야 한다.

---

## 4. 비기능 및 데이터 요구사항 (NFR & Data)

### 4.1 성능 (Performance)
* **NFR-Perf-01:** 실시간 매칭 발생 시, 관련 알림 및 상태 업데이트는 **3초 이내**에 서버 측 처리가 완료되어야 한다.

### 4.2 보안 (Security)
* **NFR-Sec-01 (암호화):** 사용자의 비밀번호는 BCrypt 알고리즘을 사용하여 단방향 암호화 저장한다.
* **NFR-Sec-02 (데이터 접근 제어):** 구직자의 민감 정보(실명, 전화번호, 이메일)는 매칭 성사 전까지 API 응답 데이터에 포함되지 않도록 필터링해야 한다.

### 4.3 데이터 관리 (Data Constraints)
* **DB-01 (로그 보존):** 3일 재노출 로직 처리를 위해 `SWIPE_LOGS`의 Dislike 기록은 물리적으로 삭제하지 않고 유지한다.
* **DB-02 (엔티티 분리):** 확장성을 고려하여 `USERS` 테이블과 별도로 `SEEKERS`(구직자 상세), `RECRUITERS`(담당자 상세) 테이블을 1:1 관계로 분리하여 설계한다.
* **DB-03 (관계 매핑):** `MATCHES` 엔티티는 구직자 ID와 채용공고 ID를 외래키로 가지며, `CHAT_ROOMS`는 `MATCHES`와 1:1 관계를 맺는다.
