# 📄 소프트웨어 요구사항 명세서 (Software Requirements Specification)

| 프로젝트명 | CV_MOA (C_MOA) |
| :--- | :--- |
| **버전** | 2.0 (Updated) |
| **작성일** | 2026년 1월 25일 |
| **상태** | **확정 (Confirmed)** |

---

## 1. 소개 (Introduction)

### 1.1 목적 (Purpose)
본 문서는 구직자와 기업 채용 담당자를 연결하는 양방향 매칭 플랫폼 **'C_MOA'**의 기능 및 비기능 요구사항을 정의한다. 본 시스템은 기존의 정적인 채용 방식을 탈피하여, **'스와이프(Swipe)'**라는 직관적인 UI와 **'양방향 탐색(Dual Discovery)'** 모델, 그리고 **'게임화(Gamification)'** 요소를 통해 신속하고 재미있는 인재 매칭 경험을 제공하는 것을 목적으로 한다.

### 1.2 범위 (Scope)
* **구직자 (Seeker)**: 기업 공고 카드를 스와이프하여 지원 의사를 밝히고, 기업의 프리미엄 정보를 확인한다.
* **채용 담당자 (Recruiter)**: 구직자의 프로필 카드를 스와이프하여 서류 합격(스카웃) 의사를 밝히고, 인재를 탐색한다.
* **플랫폼**: 모바일 웹(Mobile Web) 환경을 우선으로 하며, 반응형 웹을 지원한다.

---

## 2. 사용자 정의 (User Roles)

| 사용자 유형 | 설명 | 주요 활동 |
| :--- | :--- | :--- |
| **구직자 (Seeker)** | 일자리를 찾는 개인 사용자 | 프로필 등록, 공고 스와이프/슈퍼라이크, 기업 정보 잠금해제 |
| **채용 담당자 (Recruiter)** | 인재를 찾는 기업 사용자 | 공고 등록, 인재 스와이프/슈퍼라이크, 채팅, 기업 정보 관리 |
| **관리자 (Admin)** | 시스템 운영자 | 부적절한 유저/공고 관리, 신고 처리 |

---

## 3. 기능 요구사항 (Functional Requirements)

### 3.1 회원가입 및 인증 (Authentication & Session)
* **FR-Auth-01 (가입)**: 시스템은 이메일 기반의 자체 회원가입을 지원하며, 초기 가입 시 활동 포인트(10,000P)를 지급한다.
* **FR-Auth-02 (기업 인증)**: '채용 담당자' 역할 가입 시, `verifiedCode` 검증을 통해 유효한 기업 담당자임을 확인한다.
* **FR-Auth-03 (세션 관리)**: Spring Security 기반의 세션 인증을 사용하며, 접근 권한(Role)에 따라 페이지 접근을 제어한다.

### 3.2 프로필 및 공고 관리 (Profile & Job Posting)
* **FR-Profile-01 (구직자 데이터)**: 구직자는 기본 인적사항 외에 `기술 스택(JSON)`, `경력 연차`, `이전 직장`, `포트폴리오 URL`을 관리한다.
* **FR-Job-01 (공고 데이터)**: 채용 담당자는 `직무 설명(JD)`, `우대 사항`, `마감일` 등을 포함한 공고를 관리한다.
* **FR-Job-02 (기업 프리미엄 정보)** `NEW`: 채용 담당자는 구직자에게 매력적으로 보일 수 있는 `면접 꿀팁`, `조직 문화`, `연봉 정보`를 입력할 수 있다. (잠금 해제 전용 콘텐츠)

### 3.3 매칭 핵심 로직 (Matching Core Logic) `UPDATED`
* **FR-Match-01 (유저 제안 - Proposal)**:
    * 기존의 단순 로그(`SWIPE_LOGS`) 방식에서 **상태 기반의 제안(`PROPOSAL`)** 방식으로 변경되었다.
    * 사용자의 스와이프 액션은 `PROPOSAL` 테이블에 `WAITING` 상태로 저장된다.
* **FR-Match-02 (제안 유형 구분)**:
    * 행동 주체에 따라 `APPLY`(구직자가 지원)와 `SCOUT`(기업이 제안) 타입으로 구분된다.
* **FR-Match-03 (상호 매칭 트리거)**:
    * 'Like' 요청 시, 상대방이 이미 나에게 보낸 'Like'(`WAITING` 상태의 Proposal)가 있는지 확인한다.
    * 존재할 경우 상태를 `ACCEPTED`로 변경하고, `MATCHES` 데이터를 생성하여 매칭을 성사시킨다.
    * 거절(Dislike) 시 상태를 `REJECTED`로 변경하여 추후 재노출되지 않도록 처리한다.

### 3.4 게임화 및 경제 시스템 (Gamification) `NEW`
* **FR-Game-01 (포인트 시스템)**: 모든 사용자는 활동 포인트(Point)를 보유하며, 특정 액션 수행 시 차감된다.
* **FR-Game-02 (슈퍼 라이크 - Super Like)**:
    * 일반 'Like'와 달리 상대방에게 강력한 호감을 표시하는 기능.
    * 사용 시 **100 포인트**가 차감되며, 상대방의 '받은 제안' 목록 최상단에 강조된 UI(금색 테두리/뱃지)로 노출된다.
* **FR-Game-03 (정보 잠금 해제 - Unlock)**:
    * 구직자는 기업의 프리미엄 정보(면접 팁, 연봉 등)를 볼 때 **50 포인트**를 사용하여 잠금을 해제할 수 있다.
    * 잠금 해제 시 AJAX 요청을 통해 즉시 정보를 복호화(블러 제거)하여 보여준다.

### 3.5 탐색 및 조회 (Discovery)
* **FR-Discovery-01 (카드 스택 UI)**:
    * 자신의 상태(구직자/담당자)에 따라 적절한 상대방 카드를 보여준다.
    * 이미 상호작용(Like/Dislike)한 대상은 목록에서 제외된다.
* **FR-Discovery-02 (받은 제안함)**:
    * 나에게 도착한 'Like' 목록을 확인한다.
    * '슈퍼 라이크'를 보낸 상대는 목록 상단에 우선 노출된다.

### 3.6 채팅 시스템 (Chat System)
* **FR-Chat-01 (방 생성)**: 매칭(`MATCHES`) 성사 즉시 채팅방이 생성된다.
* **FR-Chat-02 (실시간 대화)**:
    * `WebSocket (STOMP)` 프로토콜을 사용하여 실시간 메시지 전송 및 수신을 지원한다.
    * 상대방의 온라인 여부와 상관없이 메시지 이력이 DB에 저장되어 지속성이 보장된다.

---

## 4. 비기능 및 데이터 요구사항 (NFR & Data)

### 4.1 데이터 모델링 (Data Constraints) `UPDATED`
* **DB-01 (매칭 구조 변경)**: `SWIPE_LOGS` 테이블을 제거하고 `PROPOSALS` 테이블로 대체하여, 매칭 프로세스를 '로그 분석'이 아닌 '상태 관리' 모델로 전환한다.
* **DB-02 (프리미엄 콘텐츠)**: `RECRUITERS` 테이블에 `interview_tips`, `company_culture`, `salary_info` 컬럼을 추가한다.
* **DB-03 (사용자 자산)**: `USERS` 테이블에 `point` 컬럼을 추가하고, 동시성 이슈를 고려한 차감 로직을 적용한다.

### 4.2 기술 스택 (Tech Stack) `Confirmed`
* **Backend**: Java 17, Spring Boot 3.2, Spring Data JPA, Spring Security
* **Frontend**: Thymeleaf, Bootstrap 5, Vanilla JS (Swipe Logic)
* **Database**: MySQL 8.0
* **Real-time**: Spring WebSocket (STOMP)

---

## 📝 변경 이력 (Change Log) v2.0
1.  **매칭 로직 고도화**: 단순 로그 적재 방식에서 `Proposal` 엔티티 기반의 상태 관리(Waiting/Accepted) 방식으로 변경.
2.  **게임화 요소 추가**: 포인트 시스템, 슈퍼 라이크(100P), 정보 잠금 해제(50P) 기능 명세 추가.
3.  **데이터 상세화**: 구직자(이전 직장), 기업(프리미엄 정보, 평점, 산업군) 데이터 필드 추가.
4.  **기술 스택 확정**: Python -> Java/Spring, DB -> MySQL로 확정 반영.
